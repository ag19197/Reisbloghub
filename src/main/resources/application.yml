spring:
  application:
    name: ReisBlogHub                     # ????????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver    # ?? MySQL ???
    url: jdbc:mysql://localhost:3306/reisbloghub?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234567
  redis:
    host: localhost
    port: 6379
    password: 123456
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s    # ??????
  jackson:
    default-property-inclusion: non_null    # JSON ?????? null ???

mybatis-plus:
  type-aliases-package: com.Reisbloghub.entity   # ?????????
  global-config:
    db-config:
      id-type: auto                        # ?????????????????? @TableId ???
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # ?? SQL ????????????

logging:
  level:
    com.reisbloghub: debug                  # ??????????